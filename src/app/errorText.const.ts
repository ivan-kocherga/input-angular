export const minlength = 2;
export const maxlength = 15;

const errorTextConst = {
  required: 'Its filed is required',
  minlength: `Min length is ${minlength}`,
  maxlength: `Min length is ${maxlength}`,
  email: 'Not correct email',
};

export default errorTextConst;
